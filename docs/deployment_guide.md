# Deployment Guide

## Quick Start
1. Clone repository
2. Install dependencies: `pip install -r requirements.txt`
3. Set up environment variables
4. Run: `python main.py`
5. Open `powerbi/NVIDIA_Moat_Analysis.pbix`

## Environment Setup
- Python 3.8+
- Power BI Desktop
- Required packages in `requirements.txt`
